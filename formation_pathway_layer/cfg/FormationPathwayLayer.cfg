#!/usr/bin/env python

PACKAGE = "formation_pathway_layer"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

# For integers and doubles:
#       Name                    Type      Reconfiguration level
#       Description
#       Default  Min  Max


gen.add("enabled", bool_t, 0, "Whether to apply the formation path layer plugin or not", True)
exit(gen.generate(PACKAGE, "formation_pathway_layer_namespace", "FormationPathwayLayer"))